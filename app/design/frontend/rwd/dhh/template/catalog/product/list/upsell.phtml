<?php

if(count($this->getItemCollection()->getItems())) {
  //$_columnCount = $this->getColumnCount();
  $_columnCount = 1; // Override, haha
  ?>
  <div class="box-collateral box-up-sell">
    <h2>Denkt u ook eens aan</h2>
    <ul class="products-grid products-grid--max-<?php echo $_columnCount; ?>-col" id="upsell-product-table">
      <?php
      $this->resetItemsIterator();
      $img_size = 150;
      foreach ($this->getItemCollection()->getItems() as $_link) {
        ?>
        <li>
          <a href="<?php echo $_link->getProductUrl() ?>" title="<?php echo $this->escapeHtml($_link->getName()) ?>" class="product-image">
            <img class="lazy" data-src="<?php echo $this->helper('catalog/image')->init($_link, 'small_image')->resize($img_size) ?>" width="<?php echo $img_size;?>" height="<?php echo $img_size;?>" alt="<?php echo $this->escapeHtml($_link->getName()) ?>" />
          </a>
          <span class="proawname">
            <a href="<?php echo $_link->getProductUrl() ?>" title="<?php echo $this->escapeHtml($_link->getName()) ?>"><?php echo $this->escapeHtml($_link->getName()) ?></a>
          </span>
          <?php echo $this->getPriceHtml($_link, true, '-upsell') ?>
          <?php echo $this->getReviewsSummaryHtml($_link) ?>
        </li>
      <?php
      }
      ?>
    </ul>
  </div>
<?php
}
